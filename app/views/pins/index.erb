<div class="header">
  <h1>Learning to Code</h1>
  <h3>A Collection of Ruby on Rails Resources for Beginners</h3>
</div>
<hr/>
<div class="pins">
  <% @pins.each_slice(3) do |row| %>
    <div class="row">
      <% row.each do |pin| %>
        <div class="pin-container col-md-4">
          <div class="pin">
            <div class="pin-header row">
              <div class="logo-thumbnail col-xs-3">
                <% if pin.image.exists? %>
                  <img src="<%= pin.image.url(:thumb) %>">
                <% elsif "#{pin.category_id}" == "2" %>
                  <%= image_tag('rails-logo-thumbnail.png') %>
                <% elsif "#{pin.category_id}" == "1" %>
                  <%= image_tag('ruby-logo-thumbnail.png') %>
                <% end %>
              </div>
                <div class="col-xs-9">
                  <h4>
                    <%= link_to "#{pin.title}", pin_by_name_path(slug: pin.slug), html_options = {target: "_blank", class: "url"} %> <!-- SK ans code "pin_name_show_path" -->
            <!--      <a href="<%= pin_by_name_path(slug: pin.slug) %>"
                      <%= "#{pin.title}" %>
                      </a> --> <!-- refactored with url helpers -->
                  </h4>
                  <%= link_to "#{pin.url.truncate(30)}", "#{pin.url}", html_options = {target: "_blank", class: "url"} %>
            <!--   <a class="url" href="<%= "#{pin.url}" %>" target="_blank">
                    <%= "#{pin.url.truncate(30)}" %>
                  </a> -->
                </div>
              <hr/>
              <p class="text"><%= "#{pin.text}" %></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
<hr/>
